# Causal_Inference_Project

## Requirements & Dependencies

For running the project you need the following Python (>=3.9) packages. For the installation you can use the attatched `requirements.txt` file.

Since Tigramite is constantly being updated on GitHub, instead of using `pip install tigramite`, it’s worth noting in the README that the safest option is to install it directly from the official GitHub repository (as shown by the `git+...` line in the list above), because the PyPI version sometimes lags behind the latest PCMCI+ updates.

```bash
pip install -r requirements.txt

